## What is a memory leak, and why is it dangerous in an embedded system?

## What is a pointer? How are pointers useful in embedded programming?

## Why can’t we take the address of a register variable?

## Can you combine const and volatile? What does that mean?

## What are Interrupt Service Routines (ISRs)?

## Can ISR call another ISR? Should it be given this choice?

## Why are bitwise operators important in embedded systems?

## What does shifting (<<, >>) do in bitwise operations?

## What does static mean when applied to a function?

## What is an inline function and why might it be used in embedded code?

## What is the difference between a structure and a union?

## Why should global variables be used sparingly in embedded systems?

## What is a watchdog timer and how do you service it?

## How would you implement a simple delay function without using delay() or a library?

## What are the common issues when interfacing sensors via I2C or SPI in C?
